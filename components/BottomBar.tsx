import { Box } from "@mantine/core";
import React from "react";

export default function BottomBar(props: any) {
  return (
    <Box
      display={{
        base: "block",
        md: "none",
      }}
    ></Box>
  );
}
